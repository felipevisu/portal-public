fragment Entry on Entry {
  id
  name
  slug
  entryType {
    id
    name
    slug
  }
  documentNumber
  categories {
    id
    name
    slug
  }
  documents {
    totalCount
  }
  attributes {
    attribute {
      id
      name
    }
    values {
      id
      name
    }
  }
}
