fragment Entry on Entry {
  id
  name
  slug
  type
  documentNumber
  categories {
    id
    name
    slug
  }
  documents {
    totalCount
  }
  attributes {
    attribute {
      id
      name
    }
    values {
      id
      name
    }
  }
}
